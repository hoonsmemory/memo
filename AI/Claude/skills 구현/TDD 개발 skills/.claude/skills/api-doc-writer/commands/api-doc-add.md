# /api-doc-add

목적
- 도메인 문서에 엔드포인트(섹션)를 추가/수정하는 초안을 만든다.

사용자에게 받을 정보(아는 만큼)
- 도메인명
- 엔드포인트 제목
- 메서드/경로
- 인증 필요 여부 + 역할 제한
- 요청 DTO와 필드(필수/형식/제약)
- 성공 응답(상태코드/헤더/본문)
- 정책(정렬/페이징/유일성/은닉 등)

작동 규칙
- rules/*.md 기본값을 적용한다.
- 문서/테스트에 영향 큰 항목만 최소 질문으로 보완한다.
- templates/endpoint.md 포맷으로 출력한다.
